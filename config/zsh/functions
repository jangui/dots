#!/usr/bin/zsh

rm_crap() {
    rm -rf $HOME/Dropbox/.dropbox.cache/*
}

#move files to trash (cronjob will clean trash periodically)
del() {
  for filename in "$@" ; do
    #check if file exists
    if [ ! -f $filename ] ; then
      if [ ! -d $filename ] ; then
        echo "No such file or directory: $filename"
        break
      fi
    fi
    
    #check if file in trash w/ same name
    if [[ $(find $HOME/.trash/ -name "$filename" | wc -l) = 0 ]] ; then
      newName=$filename
    else
      newName=$filename.$(date +"%s")
    fi

    #add to trash
    if mv $filename $HOME/.trash/$newName --force ; then
      echo "$filename moved to trash"
    else
      if sudo mv $filename $HOME/.trash/$newName --force ; then
        echo "$filename moved to trash"
      else 
        echo "failed to move $filename to trash"
      fi
    fi
  done 
}
